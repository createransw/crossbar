# Потоковый кроссбар
Потоковый кроссбар должен обеспечивает коммутацию S_DATA_COUNT master-устройств и
M_DATA_COUNT slave-устройств. Выбор slave-устройства, на которое должен быть передан запрос, производится по значению сигнала s_dest_i. Если два
master-устройства обращаются к одному slave-устройству, то выбор между ними производится в соответствии
с политикой Round-Robin. Если два master-устройства обращаются к различным slave-
устройствам, то транзакции должны проходят параллельно - не блокируют друг друга.
Сигнал m_id_o принимает значение номера входного потока, с которого получена выходная
транзакция.
Сигналы `valid` говорят о готовности данных к передаче, а сигналы `ready` об успешном получении данных.
## Описание
Подробное описание модулей и принципы их работы в файле project.pdf
## Тестирование
`make test`  
Необходимо установить адрес ModelSim в пременную VSIM_PATH в makefile
## Синтез
`make compile`  
Необходимо установить адрес Quartus в QUARTUS_PATH в makefile  
Готовые отчеты о синтезе находятся в /reports
